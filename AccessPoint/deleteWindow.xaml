<Window x:Name="deleteWin" x:Class="AccessPoint.deleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="delWindow" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dbDataGrid" Margin="0,50,0,50" />
        <Button x:Name="delButton" Content="Delete" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Bottom" Width="120" Height="30"/>
        <Button x:Name="backButton" Content="Back" HorizontalAlignment="Left" Margin="10,0,0,10" VerticalAlignment="Bottom" Width="120" Height="30" Click="backButton_Click" />

    </Grid>
</Window>